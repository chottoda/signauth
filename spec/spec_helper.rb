# encoding: utf-8
$:.unshift(File.dirname(__FILE__) + '/../lib')

require 'rubygems'
require 'rspec'

RSpec.configure do |config|

  config.before(:each) do
  end

  config.after(:each) do
  end

end
